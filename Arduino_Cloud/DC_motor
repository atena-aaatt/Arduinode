/* 
  Sketch generated by the Arduino IoT Cloud Thing "dcMotorKontrolu"
  https://create.arduino.cc/cloud/things/6f32d497-d022-4ca4-8e43-46846ad02624 

  Arduino IoT Cloud Variables description

  The following variables are automatically generated and updated when changes are made to the Thing

  int motorHiz;
  bool motorYon;

  Variables which are marked as READ/WRITE in the Cloud Thing will also have functions
  which are called when their values are changed from the Dashboard.
  These functions are generated with the Thing and added at the end of this sketch.
*/

#include "thingProperties.h"

byte motorA=2;
byte motorB=4;
byte motorEn=6;

void setup() {

  pinMode(motorA, OUTPUT);
  pinMode(motorB, OUTPUT);
  pinMode(motorEn, OUTPUT);
 
  Serial.begin(9600);
 
  delay(1500); 


  initProperties();


  ArduinoCloud.begin(ArduinoIoTPreferredConnection);
  
  setDebugMessageLevel(2);
  ArduinoCloud.printDebugInfo();
}

void loop() {
  ArduinoCloud.update();
  // Your code here 
  
  
}


void onMotorHizChange()  {
  analogWrite(motorEn, motorHiz);
  Serial.print("Yeni motor hizi: ");
  Serial.println(motorHiz);

}


void onMotorYonChange()  {
  if(motorYon){
    digitalWrite(motorA, HIGH);
    digitalWrite(motorB, LOW);
    Serial.println("Yon: ileri");
  }
  else{
    digitalWrite(motorB, HIGH);
    digitalWrite(motorA, LOW);
    Serial.println("Yon: geri");
  }
  
}
